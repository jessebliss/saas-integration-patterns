# Integration Topologies

ASCII diagrams for common SaaS integration topologies.

## 1. Polling (Pull) Sync

```
┌─────────────┐                         ┌─────────────┐
│   Your     │  GET /v1/resources       │   SaaS      │
│   Sync     │  ?since=<cursor>         │   API       │
│   Job      │ ───────────────────────▶│              │
│            │ ◀─────────────────────── │  200 + data │
│            │  (paginated)             │              │
└─────────────┘                         └─────────────┘
      │
      │ upsert by provider id
      ▼
┌─────────────┐
│   Your DB   │
└─────────────┘
```

## 2. Webhook + Async Processing

```
  SaaS Provider              Your API              Queue              Worker
       │                         │                    │                   │
       │  POST /webhooks/events  │                    │                   │
       │ ──────────────────────▶│                    │                   │
       │                         │  enqueue by        │                   │
       │                         │  event_id          │                   │
       │                         │ ──────────────────▶                   │
       │  200 OK                 │                    │  dequeue          │
       │◀───────────────────────│                    │ ◀────────────────│
       │                         │                    │                   │ process
       │                         │                    │                   │ idempotent
```

## 3. Hybrid: Webhooks + Periodic Reconciliation

```
                    ┌──────────────────────────────────────┐
                    │  Event stream (webhooks)             │
                    │  → real-time updates                 │
                    └───────────────┬──────────────────────┘
                                    │
                                    ▼
┌─────────────┐              ┌─────────────┐              ┌─────────────┐
│   SaaS      │   webhook    │   Your      │   reconcile  │   Your      │
│   Provider  │ ────────────▶│   System    │◀──────────── │   Sync Job  │
│             │              │   (queue +  │   (scheduled │   (pull     │
│             │   pull sync  │    workers) │    full/delta│    from API)│
│             │ ◀────────────│             │               │ ────────────▶│
└─────────────┘              └─────────────┘              └─────────────┘
```

## 4. Outbound API Calls (Your System → Provider)

```
┌─────────────┐   Idempotency-Key + body    ┌─────────────┐
│   Your      │ ──────────────────────────▶│   Provider  │
│   System    │                             │   API       │
│             │ ◀────────────────────────── │             │
│             │   200 + resource or 429     └─────────────┘
└─────────────┘
       │
       │ retry with same Idempotency-Key on 5xx/429
       ▼
```
